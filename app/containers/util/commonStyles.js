import { redVariant } from "./colors";
import { StyleSheet } from "react-native";
export const commonStyles = StyleSheet.create({
  cancelBooking: {
    fontSize: 14,
    fontWeight: "400",
    color: redVariant,
    fontFamily: "Nunito-400",
  },
});

